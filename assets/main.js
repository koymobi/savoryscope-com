const menuButton=document.querySelector('[data-menu]');const nav=document.querySelector('[data-nav]');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));});}
const panel=document.querySelector('[data-search-panel]');const input=document.querySelector('[data-search-input]');const results=document.querySelector('[data-search-results]');function render(q=''){const term=q.trim().toLowerCase();const list=(window.SAVORY_ARTICLES||[]).filter(a=>!term||a.title.toLowerCase().includes(term)||a.category.toLowerCase().includes(term)).slice(0,10);results.innerHTML=list.length?list.map(a=>'<a class="search-result" href="'+a.url+'"><small>'+a.category+'</small>'+a.title+'</a>').join(''):'<p class="search-empty">No matching stories found.</p>';}document.querySelectorAll('[data-search-open]').forEach(b=>b.addEventListener('click',()=>{panel.classList.add('open');panel.setAttribute('aria-hidden','false');render();setTimeout(()=>input.focus(),0);}));document.querySelector('[data-search-close]')?.addEventListener('click',()=>{panel.classList.remove('open');panel.setAttribute('aria-hidden','true');});input?.addEventListener('input',e=>render(e.target.value));panel?.addEventListener('click',e=>{if(e.target===panel){panel.classList.remove('open');panel.setAttribute('aria-hidden','true');}});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&panel?.classList.contains('open')){panel.classList.remove('open');panel.setAttribute('aria-hidden','true');}});
document.querySelectorAll('img.optimized-image').forEach(img=>{const reveal=()=>img.classList.add('is-loaded');if(img.complete){reveal();}else{img.addEventListener('load',reveal,{once:true});img.addEventListener('error',reveal,{once:true});}});
